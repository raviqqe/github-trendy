{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": [
      "npm run deps",
      "npm run lint",
      "npm run test",
      "npm run build"
    ]
  },
  "hosting": {
    "public": "app/dist"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
